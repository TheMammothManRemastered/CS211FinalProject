package rootPackage.Battle.AI;

import rootPackage.Battle.Actions.Action;

import java.util.List;

/**
 * Clarify the AILevel4
 * That has stuff about status effects, which I don't know what to do
 */

public class AIBoss extends AI {
    @Override
    public Action determineBestMove(List<Action> validActions) {
        return null;
    }
}
